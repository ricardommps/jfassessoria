import { Typography } from '@mui/material';

import { Box } from './styles';
export default function NotMobile() {
  return (
    <Box>
      <Typography>
        Página de destino para dispositivos móveis indisponível. Acesse por um computador
      </Typography>
    </Box>
  );
}
