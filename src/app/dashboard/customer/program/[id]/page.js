import { CustomerProgramView } from 'src/sections/customer/view';

export const metadata = {
  title: 'Dashboard: Programas / Treinos',
};

export default function Page() {
  return <CustomerProgramView />;
}
